meta:
  structure: tree | chain
  type: fuzzy

# limitations:
#  tree = any expression must be last in chain
#  chain = more complex tree without limitations, gives access to previous word in chain

tpl:
  - get $(ls /foo) $(ls /bar)

kbe:
  - $(ls /foo)